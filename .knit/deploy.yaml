version: 1
on:
  push:
    deployments:
      - target: dev
        cwd: /mnt/pvc/gluster-repository-http
        timeoutMs: 300000
        commands:
          - git pull --ff-only
          - .knit/validate-publication.sh
